framework:
    rate_limiter:
        api:
            policy: 'sliding_window'
            limit: 100
            interval: '1 minute'

        login:
            policy: 'sliding_window'
            limit: 5
            interval: '1 minute'

        resend_verification:
            policy: 'sliding_window'
            limit: 3
            interval: '15 minutes'

        ai_question:
            policy: 'sliding_window'
            limit: 10
            interval: '1 minute'

        message_send:
            policy: 'sliding_window'
            limit: 20
            interval: '1 minute'

        review_create:
            policy: 'sliding_window'
            limit: 5
            interval: '10 minutes'

        client_request_create:
            policy: 'sliding_window'
            limit: 5
            interval: '10 minutes'

        vendor_resume_upload:
            policy: 'sliding_window'
            limit: 4
            interval: '15 minutes'

        delivery_upload:
            policy: 'sliding_window'
            limit: 6
            interval: '10 minutes'
