# Copy this file to .env.test.local when you have a dedicated test database.
# .env.test.local is gitignored by Symfony defaults.
# Important: TEST_DATABASE_URL must not point to the same DB as DATABASE_URL.
# After setting this, run:
#   composer test:prepare-db
#   composer test:api:isolated

TEST_DATABASE_URL="mysql://marketplace_user:Jay442tx@127.0.0.1:3306/marketplace_test?serverVersion=mariadb-11.8.5&charset=utf8mb4"
